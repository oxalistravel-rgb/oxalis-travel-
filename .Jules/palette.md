## 2024-09-04 - Allow viewport zooming
**Learning:** Setting `maximum-scale=1` or `user-scalable=no` in the viewport meta tag prevents users from zooming on mobile devices, which violates WCAG accessibility guidelines.
**Action:** Always ensure the viewport meta tag allows scaling to accommodate users with visual impairments.
