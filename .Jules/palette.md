## 2024-05-24 - Viewport Zoom Accessibility
**Learning:** Restricting viewport zoom with `maximum-scale=1` or `user-scalable=no` prevents users with low vision from magnifying the content, violating WCAG accessibility guidelines.
**Action:** Always ensure viewport meta tags allow zooming by omitting `maximum-scale` and `user-scalable` properties.
