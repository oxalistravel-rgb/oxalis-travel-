## 2026-09-01 - Viewport Zoom Accessibility
**Learning:** Using `maximum-scale=1` in the viewport meta tag prevents users with visual impairments from zooming in on the content, which violates WCAG guidelines.
**Action:** Always ensure the viewport meta tag allows scaling (`content="width=device-width, initial-scale=1.0"`) and avoid restricting zoom capabilities.
